import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  drawerImage: {
    marginTop: 25,
    height: 60,
    width: 60,
    display: 'flex',
    alignSelf: 'center',
  },
});

export default styles;
